/// Where the community lives, in one place.
///
/// Two addresses and they are not interchangeable: the invite opens a room
/// where everyone can read what was already answered, and the handle is a
/// private message to one person. The site offers both because they answer
/// different needs — "tem alguém usando isso?" and "quero falar com você".
///
/// One constant each, because the invite is printed in the front page's notice
/// and again in the footer, and an invite that rots in one of the two places
/// is worse than none: a dead Discord link reads as a dead project.
const discordInvite = 'https://discord.gg/n5v3WdSJyg';

/// Shown as text, never linked — Discord has no URL that opens a direct
/// message to a username, so a link here would go nowhere.
const discordHandle = 'duh3972';
